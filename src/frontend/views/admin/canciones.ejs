<%- include('../partials/header', { title: 'Administrar Canciones' }) %>

<main class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 pt-8 pb-32">
  <div class="flex items-center mb-6 space-x-4">
    <a href="/admin" class="text-gray-400 hover:text-white transition">← Volver</a>
    <h2 class="text-2xl font-bold text-white">Administrar Canciones</h2>
  </div>

  <div class="space-y-8">
    <form id="form-insert-cancion" enctype="multipart/form-data" class="bg-[#181818] p-6 rounded-lg border border-[#282828]">
      <h4 class="text-xl font-semibold text-white mb-4 border-b border-gray-700 pb-2">Crear Canción</h4>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Nombre</label>
        <input type="text" name="nombre" required class="w-full bg-[#282828] border border-transparent rounded px-3 py-2 text-white focus:outline-none focus:ring-1 focus:ring-[#1db954] transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Álbum</label>
        <select name="album" required class="w-full bg-[#282828] border border-transparent rounded px-3 py-2 text-white focus:outline-none focus:ring-1 focus:ring-[#1db954] transition appearance-none">
          <option value="">Cargando álbumes...</option>
        </select>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Imagen (Portada)</label>
        <input type="file" name="imagen" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-[#1db954] file:text-black hover:file:bg-green-600 transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Archivo MP3</label>
        <input type="file" name="archivo" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-gray-600 file:text-white" />
      </div>
      <button class="bg-[#1db954] hover:bg-green-600 text-black font-bold py-2 px-6 rounded-full transition">Crear</button>
    </form>

    <form id="form-put-cancion" enctype="multipart/form-data" class="bg-[#181818] p-6 rounded-lg border border-[#282828]">
      <h4 class="text-xl font-semibold text-white mb-4 border-b border-gray-700 pb-2">Actualizar Canción (PUT)</h4>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">ID Canción</label>
        <input type="number" id="put-cancion-id" required class="w-full bg-[#282828] border border-transparent rounded px-3 py-2 text-white focus:outline-none focus:border-blue-500 transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Nombre</label>
        <input type="text" name="nombre" required class="w-full bg-[#282828] border border-transparent rounded px-3 py-2 text-white focus:outline-none focus:border-blue-500 transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Álbum</label>
        <select name="album" required class="w-full bg-[#282828] border border-transparent rounded px-3 py-2 text-white focus:outline-none focus:border-blue-500 transition appearance-none">
          <option value="">Cargando álbumes...</option>
        </select>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Imagen</label>
        <input type="file" name="imagen" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-gray-600 file:text-white" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Archivo MP3</label>
        <input type="file" name="archivo" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-gray-600 file:text-white" />
      </div>
      <button class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded transition">Actualizar (PUT)</button>
    </form>

    <form id="form-patch-cancion" enctype="multipart/form-data" class="bg-[#181818] p-6 rounded-lg border border-[#282828]">
      <h4 class="text-xl font-semibold text-white mb-4 border-b border-gray-700 pb-2">Actualizar Canción (PATCH)</h4>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">ID Canción</label>
        <input type="number" id="patch-cancion-id" required class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-yellow-500 transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Nombre</label>
        <input type="text" name="nombre" class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-yellow-500 transition" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Álbum</label>
        <select name="album" class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-yellow-500 transition appearance-none">
          <option value="">(Sin cambiar)</option>
        </select>
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Imagen</label>
        <input type="file" name="imagen" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-gray-600 file:text-white" />
      </div>
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-400 mb-1">Archivo MP3</label>
        <input type="file" name="archivo" class="w-full bg-[#282828] text-gray-300 rounded px-3 py-2 file:mr-4 file:py-1 file:px-3 file:rounded file:border-0 file:bg-gray-600 file:text-white" />
      </div>
      <button class="bg-yellow-500 hover:bg-yellow-600 text-black font-bold py-2 px-6 rounded transition">Actualizar (PATCH)</button>
    </form>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <form id="form-delete-cancion" class="bg-[#181818] p-6 rounded-lg border border-red-900/30">
        <h4 class="text-xl font-semibold text-red-400 mb-4 border-b border-gray-700 pb-2">Eliminar Canción</h4>
        <div class="mb-4">
          <label class="block text-sm font-medium text-gray-400 mb-1">ID</label>
          <input type="number" id="delete-cancion-id" required class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-red-500 transition" />
        </div>
        <button class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-6 rounded transition w-full">Eliminar</button>
      </form>

      <form id="form-search-cancion" class="bg-[#181818] p-6 rounded-lg border border-[#282828]">
        <h4 class="text-xl font-semibold text-white mb-4 border-b border-gray-700 pb-2">Buscar Canción</h4>
        <div class="mb-4">
          <label class="block text-sm font-medium text-gray-400 mb-1">Nombre</label>
          <input type="text" name="nombre" class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-gray-400 transition" />
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium text-gray-400 mb-1">Filtrar por Álbum</label>
          <select name="album" class="w-full bg-[#282828] rounded px-3 py-2 text-white focus:outline-none focus:border-gray-400 transition appearance-none">
            <option value="">Cualquiera</option>
          </select>
        </div>
        <button class="bg-gray-600 hover:bg-gray-500 text-white font-bold py-2 px-6 rounded transition w-full">Buscar</button>
      </form>
    </div>

    <div id="resultado" class="mt-8 bg-black text-green-400 font-mono p-4 rounded-lg overflow-x-auto text-sm border border-gray-800 min-h-[100px]"></div>
  </div>
</main>

<script>
  const resultado = document.getElementById("resultado");
  const showResult = async (res) => {
    try {
      const data = await res.json();
      resultado.innerText = JSON.stringify(data, null, 2);
    } catch {
      resultado.innerText = await res.text();
    }
  };

  // Cargar Álbumes Dinámicamente
  document.addEventListener("DOMContentLoaded", async () => {
    try {
      // USAMOS GET /albumes
      const res = await fetch("/albumes");
      if (!res.ok) throw new Error("Error en la red al buscar álbumes");

      const albumes = await res.json();

      document.querySelectorAll('select[name="album"]').forEach(select => {
        const firstOption = select.querySelector('option');
        if(firstOption.value === "") {
          select.innerHTML = `<option value="">${firstOption.textContent}</option>` + albumes.map(a => `<option value="${a.id}">${a.nombre}</option>`).join('');
        }
      });
    } catch (err) {
      console.error("Error cargando álbumes", err);
      document.querySelectorAll('select[name="album"]').forEach(select => {
        select.innerHTML = `<option value="">Error al cargar álbumes</option>`;
      });
    }
  });

  document.getElementById("form-insert-cancion").addEventListener("submit", async (e) => {
    e.preventDefault(); showResult(await fetch("/canciones", { method: "POST", body: new FormData(e.target) }));
  });

  document.getElementById("form-put-cancion").addEventListener("submit", async (e) => {
    e.preventDefault();
    const id = document.getElementById("put-cancion-id").value;
    const fd = new FormData(e.target); fd.delete("id");
    showResult(await fetch("/canciones/" + id, { method: "PUT", body: fd }));
  });

  document.getElementById("form-patch-cancion").addEventListener("submit", async (e) => {
    e.preventDefault();
    const id = document.getElementById("patch-cancion-id").value;
    const fd = new FormData(e.target); fd.delete("id");
    showResult(await fetch("/canciones/" + id, { method: "PATCH", body: fd }));
  });

  document.getElementById("form-delete-cancion").addEventListener("submit", async (e) => {
    e.preventDefault();
    showResult(await fetch("/canciones/" + document.getElementById("delete-cancion-id").value, { method: "DELETE" }));
  });

  document.getElementById("form-search-cancion").addEventListener("submit", async (e) => {
    e.preventDefault();
    const body = { nombre: e.target.nombre.value, album: e.target.album.value || undefined };
    showResult(await fetch("/canciones/search", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }));
  });
</script>

<%- include('../partials/footer') %>
